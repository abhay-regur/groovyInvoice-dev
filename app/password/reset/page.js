import ResetPasswordForm from "./formComponent";

export const metadata = {
    title: 'Reset Password',
    description: '',
};

export default function ResetPassword() {

    return (
        <ResetPasswordForm />
    );
}